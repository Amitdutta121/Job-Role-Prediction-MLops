name: job-role-prediction-mlops
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - scikit-learn
      - mlflow
      - joblib
      - matplotlib
